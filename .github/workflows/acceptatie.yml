name: Acceptatie Flutter Mobile CI

on:
  push:
    branches: [ Acceptatie ]
  pull_request:
    branches: [ Acceptatie ]

jobs:
  flutter-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.4'
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get

      - name: Verify formatting
        run: dart format --output=none --set-exit-if-changed .

      - name: Run linting (dart analyze)
        run: flutter analyze

      - name: Run tests
        run: flutter test

  build-android:
    needs: flutter-checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.4'
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-release
          path: build/app/outputs/flutter-apk/app-release.apk

#  build-ios:
#    needs: flutter-checks
#    runs-on: macos-latest  # iOS bouwen vereist macOS
#    steps:
#      - uses: actions/checkout@v5
#
#      - name: Setup Flutter
#        uses: subosito/flutter-action@v2
#        with:
#          flutter-version: '3.41.4'
#          channel: 'stable'
#
#      - name: Install dependencies
#        run: flutter pub get
#
#      - name: Build iOS (geen signing)
#        run: flutter build ios --release --no-codesign
#
#      - name: Upload iOS artifact
#        uses: actions/upload-artifact@v4
#        with:
#          name: ios-release
#          path: build/ios/iphoneos/Runner.app