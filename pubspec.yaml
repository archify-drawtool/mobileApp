name: archify_app
description: Mobile app for Archify
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  image_picker: ^1.2.1
  camera: ^0.12.0
  app_settings: ^7.0.0
  lucide_flutter: ^1.0.0
  google_fonts: ^8.0.2
  flutter_secure_storage: ^9.2.4
  aad_oauth: ^1.0.1
  image: ^4.8.0
  url_launcher: ^6.3.0
  share_plus: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
